<Page
    x:Class="MyLecture.Views.DrawingBoard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyLecture.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <RelativePanel x:Name="MainPanel" 
                   Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <InkCanvas x:Name="MainCanvas"                    
                   RelativePanel.AlignBottomWithPanel="True"
                   RelativePanel.AlignLeftWithPanel="True"
                   RelativePanel.AlignRightWithPanel="True"
                   RelativePanel.AlignTopWithPanel="True"/>
        
        <Button x:Name="BoardColorButton"
                Margin="24"
                Padding="0"
                Background="Black"
                Width="40"
                Height="40"
                BorderThickness="0"
                RelativePanel.AlignTopWithPanel="True"
                RelativePanel.AlignLeftWithPanel="True" Click="BoardColorButton_Click"/>

        <Button x:Name="SlideViewButton"                
                Margin="24"
                Padding="0"
                Background="Transparent"                
                Width="40"
                Height="40"
                RelativePanel.AlignTopWithPanel="True"
                RelativePanel.AlignRightWithPanel="True">
            <Viewbox>
                <SymbolIcon Symbol="ClosePane" />
            </Viewbox>
        </Button>

        <InkToolbar TargetInkCanvas="{x:Bind MainCanvas}"
                    RelativePanel.AlignHorizontalCenterWithPanel="True"
                    RelativePanel.AlignBottomWithPanel="True"/>
    </RelativePanel>
</Page>
